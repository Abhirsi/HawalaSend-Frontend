// Minimal Dashboard.jsx to test
import React from 'react';

const Dashboard = () => {
  return (
    <div style={{ padding: '2rem' }}>
      <h1>Dashboard Test</h1>
      <p>If you see this, Dashboard component is working without router errors.</p>
    </div>
  );
};

export default Dashboard;